<Profile address> cần sửa be phần `getProfile`, `getMe`, `updateProfile`, `updateMe` trong file `ACE/src/services/AuthService.js`, `ACE/src/services/UserService.js`, `ACE/src/models/UserModel.js` , lí do là >dữ liệu `/auth/me`, `/users/me`, `/auth/me/profile`, `/users/me` hiện chưa có trường địa chỉ nên frontend không thể hiển thị hay cập nhật địa chỉ hồ sơ đúng nghĩa> , hướng giải quyết là <thêm các trường địa chỉ vào bảng `users` hoặc tách bảng `user_addresses`, trả về trong response profile và hỗ trợ cập nhật>.

<Multiple customer addresses> cần sửa be phần `createOrder`, `getOrderById`, `getOrdersByUser` trong file `ACE/src/controllers/OrderController.js`, `ACE/src/models/OrderModel.js` , lí do là >backend hiện chỉ lưu `shipAddress` dạng chuỗi trên đơn hàng, không có sổ địa chỉ khách hàng, không có địa chỉ mặc định, không có CRUD cho nhiều địa chỉ> , hướng giải quyết là <tạo module `user_addresses` gồm danh sách địa chỉ, địa chỉ mặc định, nhãn địa chỉ; khi checkout chỉ truyền `addressId` hoặc snapshot địa chỉ>.

<Payment failed customer flow> cần sửa be phần `failPayment`, `verifyVnpayReturn`, `handleVnpayIPN` trong file `ACE/src/services/PaymentService.js`, `ACE/src/services/VnpayService.js`, `ACE/src/models/PaymentModel.js` , lí do là >backend mới đánh dấu payment FAILED nhưng chưa chuẩn hóa luồng cho khách hàng như: cho phép thanh toán lại, ghi lý do thất bại, thời gian hết hạn thanh toán, trạng thái đơn tương ứng sau fail> , hướng giải quyết là <thêm `failureReason`, `expiredAt`, `retryCount`, API lấy trạng thái thanh toán hiện tại và API khởi tạo lại payment cho order PENDING>.

<Auto cancel overdue failed/pending orders> cần sửa be phần `getAllOrders`, `updateOrderStatus`, `failPayment` trong file `ACE/src/models/OrderModel.js`, `ACE/src/services/PaymentService.js` , lí do là >frontend không thể tự hủy đơn quá hạn một cách an toàn vì việc này liên quan tới đồng bộ trạng thái đơn, payment và hoàn kho> , hướng giải quyết là <định nghĩa timeout thanh toán, có job định kỳ hoặc xử lý tại thời điểm truy cập để chuyển order PENDING quá hạn sang CANCELLED và hoàn lại tồn kho>.

<Advanced order search server-side> cần sửa be phần `getAllOrders` trong file `ACE/src/controllers/OrderController.js`, `ACE/src/models/OrderModel.js` , lí do là >admin hiện chỉ filter theo `status`; các lọc nâng cao như khoảng ngày, tổng tiền, khách hàng, phone, email, địa chỉ, payment status đang phải làm ở frontend sau khi load danh sách lớn> , hướng giải quyết là <mở rộng query params cho `search`, `dateFrom`, `dateTo`, `minTotal`, `maxTotal`, `paymentStatus` và query SQL tương ứng>.

<Analytics by date range and payment/order status> cần sửa be phần `DashboardController`, `DashboardService`, `DashboardModel` trong file `ACE/src/controllers/DashboardController.js`, `ACE/src/services/DashboardService.js`, `ACE/src/models/DashboardModel.js` , lí do là >frontend hiện phải tự tổng hợp toàn bộ đơn để làm báo cáo từ ngày A đến ngày B; cách này không tối ưu và không đủ tin cậy khi dữ liệu lớn> , hướng giải quyết là <tạo endpoint thống kê theo `from`, `to` trả về số lượng đơn theo trạng thái và doanh thu theo tình trạng đơn/thanh toán đã tổng hợp sẵn từ backend>.
